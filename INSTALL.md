El juego está hecho con GDX, usando Gradle para scaffolding y manejo de dependencias.

El código se encuentra en `core/src/com/frontanilla/`. Para correrlo, desde la raíz del juego correr el comando:

$ ./gradlew desktop:run